country_decisions = {
	water_crisis_info = {
		color = { 10 100 255 }
		major = yes
		potential = {
			has_country_flag = has_water_crisis
		}
		allow = {
			hidden_trigger = {
				has_country_flag = enact_water_crisis_button
			}

			custom_trigger_tooltip = {
				tooltip = water_crisis_tooltip
			}
		}

		effect = {
			
		}
	}

	purchase_arapleen_ruler = {
		color = { 237 227 43 }
		major = yes
		potential = {
			is_subject_of_type = arapaleen_subject_type
		}
		allow = {
			hidden_trigger = {
				
				had_country_flag = {
					flag = recent_arapleen_ruler_purchase
					days = 7300
				}
			}
			if = {
				limit = { ai = no }
				NOT = {
					num_of_loans = 1
				}
			}
			
			variable_arithmetic_trigger = {
				custom_tooltip = has_more_money_than_arapleen_ruler

				any_country = {
					has_country_flag = is_arapleen_ruler
					variable_arithmetic_trigger = {
						export_to_variable = {
							which = my_money
							value = treasury
							who = ROOT
						}
						export_to_variable = {
							which = his_money
							value = trigger_value:monthly_income
						}
						multiply_variable = { which = his_money value = 36 }
						check_variable = { which = my_money which = his_money }
					}
				}
			}

			custom_trigger_tooltip = {
				tooltip = GetArapleenRulerPurchase
			}
		}
		
		effect = {

			every_country = {
				limit = {
					has_reform = sahara_arapleen_monarchy
				}
				clr_country_flag = recent_arapleen_ruler_purchase
				if = {
					limit = {
						NOT = {
							tag = THIS
						}
					}
					create_subject = {
						subject_type = arapaleen_subject_type
						subject = THIS
					}
				}
				set_country_flag = recent_arapleen_ruler_purchase
			}
			
			
		}
	}
}
