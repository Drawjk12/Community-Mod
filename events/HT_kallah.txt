namespace = kallah_events

#Prayer
country_event = {
	id = kallah_events.1
	title = kallah_events.1.t
	desc = kallah_events.1.d
	picture = PRAYING_eventPicture
	
	is_triggered_only = yes

	trigger = {
		religion_group = kallah
		NOT = { has_country_flag = daily_prayers  }
	}
	
	immediate = {
		set_country_flag = daily_prayers
	}

	option = {
		name = kallah_events.1.a
		add_karma = 10
		add_estate_loyalty = {
			estate = estate_church
			loyalty = 10
		}
	}
	option = {
		name = kallah_events.1.b
		trigger = {
			religion = meku
		}
		add_karma = -10
		add_estate_loyalty = {
			estate = estate_church
			loyalty = -10
		}
	}
	option = {
		name = kallah_events.1.b
		trigger = {
			religion = orthodox_meku
		} 
		add_karma = -10
		add_estate_loyalty = {
			estate = estate_church
			loyalty = -20
		}
	}
}

#Karma
country_event = {
	id = kallah_events.2
	title = kallah_events.2.t
	desc = kallah_events.2.d
	picture = DEBATE_REPUBLICAN_eventPicture
	
	is_triggered_only = yes
	
	trigger = {
		religion_group = kallah
	}
	
	immediate = {
		set_country_flag = daily_prayers
	}

	option = {
		name = kallah_events.2.a
		add_karma = 10
		add_estate_loyalty = {
			estate = estate_church
			loyalty = 10
		}
	}
	option = {
		name = kallah_events.2.b
		add_karma = -10
		add_estate_loyalty = {
			estate = estate_church
			loyalty = -10
		}
	}
}
